"use strict";
let friendsNames = ["Sumaira", "Shazia", "Nadra", "Sanaa"];
console.log(friendsNames);
for (let i = 0; i < friendsNames.length; i++) {
    console.log(`Hello! Too long no see , ${friendsNames[i]}`);
}
